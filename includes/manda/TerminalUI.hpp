#ifndef MANDA_TERMINAL_UI_HPP
#define MANDA_TERMINAL_UI_HPP

#include "Game.hpp"

// Temporary terminal UI (arrow keys + enter). Replace with GUI later.
namespace TerminalUI {

void run(Game& game);

}

#endif
